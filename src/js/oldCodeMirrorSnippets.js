/* -------- CodeMirror events -------- */

// testMarker1.on("beforeCursorEnter", () => {
//   console.log("About to be clicked")
// })

// editor.on("mousedown", (cm, event) => {
//   const target = event.target
//   if (active) active.classList.remove('expand')
//   active = target
//   active.classList.add('expand')
// })

// editor.on("change", () => {
// })
